1. TypeScript এ Interface এবং Type এর মধ্যে পার্থক্য

TypeScript এ object বা data structure কেমন হবে, সেটি নির্ধারণ করার জন্য আমরা সাধারণত interface অথবা type alias ব্যবহার করি। যদিও দুইটির কাজ অনেকটাই একই মনে হয়, তবুও ব্যবহার এবং বৈশিষ্ট্যের দিক থেকে কিছু গুরুত্বপূর্ণ পার্থক্য রয়েছে।

🔹 Extension / Inheritance

Interface খুব সহজে extend করা যায়।

Type দিয়ে extend করতে চাইলে সাধারণত intersection (&) ব্যবহার করতে হয়।

Interface উদাহরণ:

interface Person {
  name: string;
}

interface Employee extends Person {
  salary: number;
}


Type উদাহরণ:

type A = { name: string };
type B = A & { salary: number };

🔹 Declaration Merging

Interface বারবার declare করলে তারা merge হয়ে যায়।

Type কখনো merge হয় না; একই নামে দুইবার define করলে error হয়।

Interface merging উদাহরণ:

interface User {
  name: string;
}

interface User {
  age: number;
}
// Result => { name: string, age: number }

## কখন কোনটা ব্যবহার করবেন?
বিষয়	Interface	Type
- Extend করা যায়	✔️	✔️
 - Merge হয়	✔️	❌
- Union / Tuple define	❌	✔️
- Best for	Object structure	Complex / hybrid types
✔️ সংক্ষেপে

Interface মূলত object shape নির্ধারণের জন্য ভালো, আর Type আরও flexible— বিশেষ করে union, intersection বা জটিল টাইপের ক্ষেত্রে।


2. any, unknown এবং never — টাইপগুলো কিভাবে আলাদা?

TypeScript এর তিনটি special টাইপ— any, unknown, এবং never। এগুলো নতুনদের কাছে একটু confusing মনে হলেও, আসলে প্রতিটির আলাদা উদ্দেশ্য আছে।

🔹 any — সবচেয়ে flexible, কিন্তু unsafe

any ব্যবহার করলে TypeScript টাইপ checking বন্ধ করে দেয়।

let data: any = "hello";
data = 42;
data = true;


যেকোনো value রাখা যায়

যেকোনো কাজ করা যায়

সবচেয়ে কম নিরাপদ টাইপ

নিয়মিত কাজে any এড়িয়ে চলাই ভালো।

🔹 unknown — নিরাপদ any

unknown এ যেকোনো value রাখা যায়, কিন্তু ব্যবহার করার আগে অবশ্যই type check করতে হয়।

let value: unknown = "Hello";

if (typeof value === "string") {
  console.log(value.toUpperCase());
}


Direct ব্যবহার করা যায় না

নিরাপদভাবে value handle করতে সাহায্য করে

🔹 never — যে value কখনোই থাকে না

never এমন function এর জন্য ব্যবহৃত হয় যেটি কখনোই শেষ হয় না অথবা কখনোই কোনো value return করে না।

function throwError(msg: string): never {
  throw new Error(msg);
}

function infiniteLoop(): never {
  while (true) {}
}


✔️ তুলনা টেবিল
টাইপ	কী বোঝায়	নিরাপদ?
- any	যেকোনো value	❌ No
- unknown	যেকোনো value, কিন্তু ব্যবহার করতে check লাগে	✔️ Yes
 - never	Function কখনো return করে না	—

## উপসংহার

any সবচেয়ে কম নিরাপদ — প্রয়োজনে ব্যবহার করবেন।

unknown নিরাপদ alternative — type check ধরে রাখতে সাহায্য করে।

never সাধারনত error handling বা infinite logic এ ব্যবহৃত হয়।